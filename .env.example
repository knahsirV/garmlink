# Garmin credentials (used for initial auth and re-auth if tokens expire)
GARMIN_EMAIL=you@example.com
GARMIN_PASSWORD=your_password

# Base64-encoded garmin_tokens.json content (run: base64 < ~/.garminconnect/garmin_tokens.json)
GARMIN_TOKENS_JSON=

# Secret token that protects your MCP endpoint (generate: openssl rand -hex 32)
MCP_AUTH_TOKEN=

# Port to listen on (Railway sets this automatically)
PORT=8000
